# 复制成 .env。
# 默认就是 mock 模式（VITE_USE_MOCK 不设置或不是 "false" 都算开），完全不需要
# 后端/数据库，npm run dev 直接能跑，登录随便填账号密码即可。
# VITE_USE_MOCK=false

# 只有关掉 mock、要接真实后端时才需要下面两个：
VITE_API_URL=http://localhost:8000
VITE_WS_URL=ws://localhost:8000
